<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full min-h-screen text-white p-10" style="background-color: lightblue;">
        <div class="flex items-end w-full">
            <a class="bg-red-500 rounded-md px-3 py-2" href="/logout">Logout</a>
        </div>
        <br>
        <h3 class="text-white text-3xl mb-5 mt-5" style="font-weight: 700; color: black; font-family: cursive, lobster; font-size: 35px;">
            <span class="font-light bg-zinc-400" style="font-family: lobster, cursive; font-weight: 700;">Hello Dear</span>, <%= user.username %> 👋
        </h3>
        <h5 class="mb-5 text-xl">You can create a new post.</h5>
        <form action="/post" method="post">
            <textarea class="resize-none bg-transparent text-xl text-black border-2 border-zinc-200 w-1/4 p-3 outline-none" style="font-weight: 500; font-family: 'Courier New', Courier, monospace;" name="content" placeholder="What's on your mind?"></textarea>
            <input class="py-2 px-3 bg-blue-600 block rounded-md mt-2 w-40" value="Create new post" type="submit">
        </form>
        <div class="post mt-20">
            <h3 class="text-zinc-800 text-2xl" style="font-weight: 700; font-family: 'Courier New', Courier, monospace;">Your posts..</h3>
            <div class="postcontainer mt-5">
                <% user.posts.reverse().forEach(function(post) { %>
                    <div class="post m-5 w-1/4 p-4 border-2 rounded-md border-zinc-800" style="background-color: antiquewhite;">
                        <h4 class="text-blue-700 text-2xl" style="font-weight: 600;">@<%= user.username %></h4>
                        <p class="text-xl text-black ml-7 mt-3" style="font-weight: 500;"><%= post.content %></p>
                        <small class="text-black ml-7 mt-5 inline-block"><%= post.likes.length %> Likes</small>
                        <div class="btns flex gap-5 ml-7">
                            <a class="text-blue-500 text-xl" style="font-weight: 400;" href="/like/<%= post.id %>">
                                <%= post.likes.indexOf(user._id) === -1 ? "like" : "unlike" %>
                            </a>
                            <a class="text-zinc-500 text-xl" style="font-weight: 400;" href="/edit/<%= post.id %>">edit</a>
                            <a class="text-red-500 text-xl" style="font-weight: 400;" href="/delete/<%= post.id %>">delete</a>
                        </div>
                    </div>
                <% }) %>
            </div>
        </div>
    </div>
</body>
</html>
